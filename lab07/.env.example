# Copy to ".env" and fill in real values. NEVER commit the real .env.
# docker compose automatically loads variables from a local .env file.

POSTGRES_DB=reminders
POSTGRES_USER=reminder_user
# Use a strong, randomly generated value in real environments.
POSTGRES_PASSWORD=change_me_use_a_secret_manager

LOG_LEVEL=info
